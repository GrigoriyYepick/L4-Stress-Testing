@page
@model StressTesting.UI.Pages.Media.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MediaData[0].MediaKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaData[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaData[0].Path)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaData[0].Width)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MediaData[0].Height)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MediaData) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MediaKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Path)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Width)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Height)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
